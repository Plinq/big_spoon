require 'rake'

begin
  require 'jeweler'
  Jeweler::Tasks.new do |gemspec|
    gemspec.name = "working_girl"
    gemspec.summary = "Adds before and after hooks to any method, because that's just how things should be"
    gemspec.description = %{
      WorkingGirl will add a hooks method to every class. Call that method with a block and add all kinds of fun hooks before and after your methods.
    }
    gemspec.email = "flip@x451.com"
    gemspec.homepage = "http://github.com/Plinq/working_girl"
    gemspec.authors = ["Flip Sasser"]
  end
rescue LoadError
end
